    <div class="tabs tabs-border py-2 justify-center max-w-lg lg:pt-6">
      <input type="radio" name="my_tabs_4" role="tab"
        class="tab text-base text-secondary hover:text-secondary"
        aria-label="<%= t('profiles.tabs.posts') %>" checked />
        <div role="tabpanel" class="tab-content md:p-1 border-secondary bg-white/80 rounded-4xl w-full">
        <div class="flex flex-col mx-auto items-center justify-center text-xs md:text-sm w-full">
            <% if @posts.present? %>
            <%= render @posts %>
                <div class="flex items-center justify-center p-4">
                <%== pagy_nav(@pagy_posts) %>
                </div>
            <% else %>
            <p class="p-4 text-stone-400"><%= t('profiles.empty_states.no_posts') %></p>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
