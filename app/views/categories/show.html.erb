<div class="p-1 xl:p-4 pb-24 xl:pb-8">
  <div class="flex flex-col p-1 lg:p-4 items-center justify-center w-full max-w-3xl mx-auto bg-base-100 rounded-4xl">
    <%= render "shared/product_list_header", title: "#{@category.name}の商品一覧" %>
    <%= render 'products/search_form', q: @q, category: @category %>
    <%= render "shared/product_list", products: @products, category: @category, product_stats: @product_stats, pagy: @pagy %>
  </div>
</div>

<%= render "shared/product_register_button" %>
